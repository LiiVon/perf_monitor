# 可视化 Linux 系统性能监控平台

独立开发者 | 2026.02 - 2026.05

项目背景：面向多服务节点集群，解决缺乏统一性能监控、异常快速定位困难的问题。

项目描述：设计并实现了一套基于 Push 架构的分布式 Linux 性能监控系统，支持多节点 CPU、内存、磁盘、网络、软中断指标采集、评分、持久化与可视化展示。系统采用 C++17 开发，结合 Linux 内核模块零拷贝采集与 eBPF TC Hook 网络监控，实现高性能指标上报；管理端通过 gRPC + HTTP 双协议对外提供数据服务，Qt 6 前端支持多主机切换与实时趋势展示。

技术栈：C++17、Linux 内核模块、mmap、eBPF/libbpf、TC Hook、gRPC、Protobuf、MySQL、Qt 6、cpp-httplib、nlohmann/json、CMake。

主要成果：
1. 自研高性能采集层，开发 `cpu_stat_collector` 内核模块，利用 `mmap` 将 CPU 统计数据零拷贝映射到用户态，结合 `/proc/stat` 回退机制，兼顾采集性能与运行稳定性。
2. 基于 `libbpf` 实现 eBPF TC Hook 网络监控，在网卡 Ingress/Egress 路径原子累加流量与包计数，支持速率计算与计数器回绕处理。
3. 设计 Push 模式分布式通信链路，Worker 周期性推送 `MonitorInfo` 至 Manager，Manager 端通过 gRPC 接收数据并完成主机识别、评分计算、变化率分析与离线节点清理。
4. 构建 MySQL 持久化与查询体系，设计 5 张业务表存储主表、网络、磁盘、内存、软中断明细及变化率数据，支持历史查询、趋势聚合与异常分析。
5. 实现多维加权健康评分模型，综合 CPU、内存、负载、磁盘、网络指标生成主机评分，为调度和异常定位提供统一参考。
6. 基于 Qt 6 + QChart 开发可视化看板，通过 HTTP/JSON 接口实时拉取最新数据，支持 6 个 Tab 页面、多主机切换、3 秒自动刷新与趋势展示。
7. 设计三层自动降级策略，eBPF 加载失败回退 `/proc/net/dev`，内核模块 open 失败回退 `/proc/stat`，确保在非特权环境或能力受限机器上仍可正常运行。

## 模拟实战面试题（含参考答案）

## 一、先说你的这段简历描述怎么样

整体评价：很强，技术深度和工程完整度都不错，已经明显超过普通校招项目。

亮点：
1. 技术链完整：内核模块 + eBPF + gRPC + MySQL + Qt，覆盖采集、传输、计算、存储、展示全链路。
2. 架构意识强：有 Worker/Manager 分层、Push 模式、多节点、评分模型、异常定位。
3. 可落地性高：不是“只采集不落库”，你做了持久化、查询和可视化。

可优化点（建议你在正式简历里补充）：
1. 增加量化指标：例如“单节点采集开销 < x% CPU”“支持 x 台节点并发推送”“UI 刷新延迟 p95 < x ms”。
2. 统一口径：描述里写“Worker 每 10s 推送”，但当前源码中 `worker/src/main.cpp` 里构造 `MonitorPusher` 用的是 5 秒间隔。面试时要提前准备解释：默认值 10 秒，可配置，演示环境改为 5 秒。
3. 补充可靠性设计：强调降级策略（eBPF/内核模块失败回退 `/proc`）、离线节点清理、异常阈值策略。
4. 给出你的主导贡献：比如“独立完成 x 个核心模块，约 x 行 C++ 代码”。

---

## 二、模拟实战面试题（题后即答案）

说明：下面答案尽量贴近你当前代码实现，同时给出可提升点，方便你在面试中“既能讲实现，也能讲优化”。

### A. 项目总览与架构

Q1：你这个系统为什么选 Push 架构，而不是 Manager 主动 Pull？

A1：
1. Push 能降低 Manager 主动轮询大量节点的连接管理和调度成本。
2. Worker 本地采集后直接上报，路径短，时效性更稳定。
3. 在代码中由 `MonitorPusher` 周期执行 `PushOnce`，通过 gRPC `SetMonitorInfo` 上报；Manager 侧 `GrpcServerImpl` 接收后回调 `HostManager::OnDataReceived`。
4. 可优化点：Push 模式下若网络抖动，需补充重试、缓冲队列、批量上报策略。

Q2：Manager 为什么同时提供 gRPC 和 HTTP 两套接口？

A2：
1. gRPC 给 Worker 上报和后端内部高性能通信用。
2. HTTP/JSON 给 Qt 前端快速接入用，降低前端对 gRPC 依赖。
3. 当前实现中 gRPC 监听 50051，HTTP 服务监听 50052，`/api/latest` 返回全主机最新数据。
4. 这种“双通道”设计平衡了性能与开发效率。

Q3：从数据流角度，讲一下一次完整链路。

A3：
1. Worker 的 `MetricCollector` 聚合各监控器（CPU、内存、磁盘、网络等）到 `MonitorInfo`。
2. `MonitorPusher` 周期调用 gRPC `SetMonitorInfo` 推送。
3. Manager `HostManager::OnDataReceived` 计算分数、计算变化率、更新内存缓存。
4. `WriteToMysql` 写入 5 张表。
5. Qt 每 3 秒请求 HTTP `/api/latest`，渲染 6 个 Tab。

Q4：你怎么定义“最佳服务器”？

A4：
1. 通过 `CalcScore` 计算 0~100 综合分，分数越高越优。
2. 维度权重：CPU 35%、内存 30%、负载 15%、磁盘 15%、网络 5%。
3. `GetBestHost` 在内存缓存 `host_scores_` 中选最高分主机。
4. 面试中可补一句：当前是静态权重，可做动态权重或按业务画像切换。

Q5：离线主机怎么处理？

A5：
1. `HostManager::ProcessLoop` 每 60 秒扫描一次缓存。
2. 若主机最后更新时间超过 60 秒则判定 stale 并移除。
3. 这样可以避免前端显示“僵尸节点”。
4. 可优化：阈值按推送周期自动计算，例如 `3 * push_interval`。

### B. 采集层设计

Q6：`MetricCollector` 是怎么组织多种采集器的？

A6：
1. 用统一接口 `MonitorInter`（多态）管理多个监控器。
2. 构造时按顺序注册：CPU load、软中断、CPU stat、内存、磁盘、主机信息、用户信息、网络。
3. `CollectAll` 遍历执行 `UpdateOnce`，最终设置 `monitor_info->set_name(hostname_)`。
4. 优点是扩展新指标时只需新增一个监控器并注册。

Q7：为什么既用 `/proc` 又用内核模块和 eBPF？

A7：
1. `/proc` 通用、兼容强，是最稳定兜底。
2. 内核模块适合高频、低开销读取内核累计值，并能做 mmap 零拷贝。
3. eBPF 适合网络路径细粒度统计，侵入小、可热加载。
4. 你项目实际做了降级：eBPF/模块失败自动回退 `/proc`。

Q8：首次采样为什么很多速率是 0？

A8：
1. 因为速率本质是差分：$rate = (curr - prev) / \Delta t$。
2. 首次没有 `prev`，所以无法计算，代码中会给 0 或跳过百分比填充。
3. 第二轮起数据才有意义，这在监控系统里是正常现象。

Q9：采集周期应该怎么定？

A9：
1. 当前演示代码 Worker 推送周期是 5 秒（构造参数），默认值是 10 秒。
2. 周期越短，时效越好但开销越大。
3. 一般建议按指标类型分层：CPU/负载 5~10 秒，磁盘/网络 1~5 秒，慢变化项 30~60 秒。
4. 可做自适应：高风险节点提高采样频率。

### C. CPU 与软中断（内核模块）

Q10：CPU 使用率是怎么算的？

A10：
1. 从两次采样的各状态累计时间做差分。
2. 总时间差：$\Delta total = \sum states_{curr} - \sum states_{prev}$。
3. 某状态占比：$percent = \frac{\Delta state}{\Delta total} \times 100\%$。
4. 代码中在 `FillCpuStatMessage` 里计算 `cpu_percent`、`usr_percent` 等字段。

Q11：你为什么做了 `/dev/cpu_stat_monitor` + mmap？

A11：
1. 目标是减少用户态频繁解析文本 `/proc/stat` 的开销。
2. 内核模块把结构化数据映射到用户态，读取更直接。
3. `CpuStatMonitor::UpdateOnce` 里优先 `open + mmap`，失败再回退 `/proc/stat`。
4. 这是性能与可靠性的折中。

Q12：软中断监控价值是什么？

A12：
1. 高网络负载、驱动问题、内核调度异常常体现在 softirq 指标突变。
2. 比如 `NET_RX` 激增可能对应收包风暴，`TIMER` 异常可能对应定时器压力。
3. 你把 softirq 细分到 CPU 维度并持久化，便于定位“哪个核异常”。

Q13：内核模块会带来哪些风险？

A13：
1. 稳定性风险：内核态 bug 可能影响系统。
2. 兼容性风险：不同内核版本结构差异。
3. 安全风险：模块权限高。
4. 应对：最小化功能、严格边界检查、失败降级到 `/proc`、灰度发布。

### D. eBPF 网络监控

Q14：为什么网络监控选择 TC hook，而不是 kprobe？

A14：
1. TC hook 挂载点更稳定，语义明确（ingress/egress）。
2. kprobe 依赖内核符号，版本兼容性通常更差。
3. 你在代码里通过 skeleton 加载后，分别附加 ingress/egress 程序。

Q15：网卡速率怎么计算？

A15：
1. eBPF map 里存累计字节/包计数。
2. 用户态读取后与上次缓存差分。
3. 用 $rate = diff / \Delta t$ 得到 B/s 和 pkt/s。
4. 代码还处理了计数器回绕（diff < 0 时回退为当前值）。

Q16：为什么跳过 lo、docker、veth？

A16：
1. 回环和临时虚拟网卡可能放大噪声。
2. 集群节点监控更关注物理或核心业务网卡。
3. 在 `GetAllIfIndexes` 里对这些前缀做了过滤。
4. 可优化：改成配置项，支持白名单/黑名单。

Q17：eBPF 失败时你怎么保证系统可用？

A17：
1. 构造 `NetEbpfMonitor` 时如果 `InitEbpf` 失败，打印告警并降级。
2. 降级路径是传统网络采集实现（`/proc/net/dev`）。
3. 这样即使内核能力不足，主流程仍可工作。

Q18：eBPF 资源清理怎么做？

A18：
1. `CleanupEbpf` 中遍历已附加网卡，执行 ingress/egress detach。
2. 销毁 skeleton，清空缓存和状态。
3. `Stop` 调用清理逻辑，保证退出时不残留 hook。

### E. gRPC / Protobuf / 通信可靠性

Q19：为什么监控数据要用 Protobuf？

A19：
1. 二进制编码更紧凑，序列化效率高。
2. Schema 演进清晰，前后兼容可控。
3. 对多语言和分布式系统很友好。

Q20：你的 gRPC 调用是同步还是异步？影响是什么？

A20：
1. 当前 Worker 侧 `PushOnce` 使用同步 RPC。
2. 优点是实现简单，失败处理直接。
3. 缺点是网络慢时会阻塞推送线程。
4. 可升级为异步 + 本地队列，提升抗抖动能力。

Q21：网络抖动导致上报失败你怎么处理？

A21：
1. 当前实现会记录错误日志并等待下个周期重试。
2. 可优化方案：指数退避、带上限重试、磁盘缓冲、批量补传。

Q22：如何保证 MonitorInfo 字段演进不影响老版本？

A22：
1. Protobuf 通过新增字段号实现向后兼容。
2. 不复用已删除字段号，尽量只追加字段。
3. 面试时可以补充“服务端忽略未知字段，客户端对缺失字段给默认值”。

### F. 评分模型与异常定位

Q23：你的评分公式是什么？

A23：
1. 先把每个维度转成 $[0,1]$ 分数（越健康越接近 1）。
2. 例：$cpu\_score = 1 - cpu\_percent/100$，$disk\_score = 1 - util/100$。
3. 负载分数按核数归一：$load\_score = 1 - load1/(cores \times 1.5)$。
4. 最终：

$$
score = 100 \times (0.35\cdot cpu + 0.30\cdot mem + 0.15\cdot load + 0.15\cdot disk + 0.05\cdot net)
$$

Q24：评分为什么要 clamp？

A24：
1. 防止异常输入导致分数越界。
2. 你实现里每个子分数和总分都做了边界约束，保证在 0~100 内。
3. 这样前端展示和排序更稳定。

Q25：异常定位依赖什么信号？

A25：
1. 当前设计有两类信号：绝对值阈值（如 CPU>80%）和变化率阈值（如 >50%）。
2. Query 层 `AnomalyThresholds` 包含 CPU/内存/磁盘/变化率配置。
3. 变化率能抓“突发异常”，绝对值能抓“持续高压”。

Q26：为什么要记录“变化率”而不只记录“原始值”？

A26：
1. 原始值适合看水平，变化率适合看突变。
2. 故障早期常是斜率变化先出现。
3. 你在主表和明细表都保存 rate 字段，为异常检测提供更早信号。

Q27：你如何解释“最佳服务器分数高但某单项偏差大”？

A27：
1. 综合分是加权结果，可能被其他高分项抵消。
2. 所以调度时应设置硬性门限，如 `disk_util < 90%` 才可选。
3. 面试里要强调“评分 + 规则”双重决策。

### G. MySQL 与查询服务

Q28：你为什么设计 5 张表，而不是一张大宽表？

A28：
1. 主表用于概览和排序，明细表按领域拆分（网络/磁盘/内存/软中断）。
2. 这样减少无关字段扫描，便于按场景查询和索引。
3. 同时保留跨表关联能力。

Q29：`server_performance` 主要存什么？

A29：
1. 核心资源指标（CPU/负载/内存/磁盘/网络）和综合评分。
2. 同时保存各关键指标变化率。
3. 有 `server_name + timestamp` 索引支撑时间范围查询。

Q30：你的分页查询怎么做？

A30：
1. 请求里带 `page` 和 `page_size`。
2. 先做 `COUNT(*)` 得总数，再 `LIMIT/OFFSET` 拉当前页。
3. `QueryService` 把总数和页信息返回前端/调用方。

Q31：趋势查询如何支持聚合？

A31：
1. 当 `interval_seconds > 0` 时，用 `UNIX_TIMESTAMP(timestamp)` 做时间桶。
2. 对每桶做 `AVG(...)` 聚合，得到分钟级/秒级趋势。
3. 不聚合时直接按时间序列返回原始点。

Q32：你当前 SQL 实现有什么风险？

A32：
1. 目前大量字符串拼接，存在 SQL 注入风险。
2. 高频写入下每次新建 MySQL 连接，连接开销偏大。
3. 改进：预编译语句 + 连接池 + 批量插入。

Q33：为什么要在明细表里也存变化率？

A33：
1. 主表变化率只能反映总体趋势。
2. 明细表变化率可定位到具体网卡/磁盘/CPU 核心。
3. 这是“异常定位”而非“仅异常发现”的关键。

### H. 并发、线程与稳定性

Q34：Manager 的并发安全是怎么做的？

A34：
1. `host_scores_` 通过互斥锁保护，读写都加锁。
2. 后台清理线程与接收线程共享该 map，也通过同一把锁避免冲突。
3. 可优化：读多写少场景可用读写锁。

Q35：当前实现可能还有哪些并发隐患？

A35：
1. `last_perf_samples`、各类 `last_*_samples` 是静态 map，当前没显式锁保护。
2. 若 gRPC 并发回调 `OnDataReceived`，理论上可能有竞态。
3. 改进：把历史缓存并入 `HostManager` 成员并统一加锁，或分片锁。

Q36：为什么 Worker 要单独推送线程？

A36：
1. 推送是周期任务，不应阻塞主线程信号处理。
2. 你通过 `MonitorPusher::Start/Stop` 管理线程生命周期。
3. 主线程只负责等待退出信号，结构清晰。

Q37：优雅退出怎么做的？

A37：
1. 捕获 `SIGINT/SIGTERM`，设置 `running=false`。
2. 主循环退出后调用 `pusher.Stop()`，join 推送线程。
3. 这样可以尽量避免资源泄露和半写状态。

### I. Qt 可视化与前后端协作

Q38：你的 Qt 看板为什么 3 秒刷新一次？

A38：
1. 监控场景需要接近实时，但不必亚秒级。
2. 3 秒刷新可以平衡流畅性与前后端压力。
3. 实现上 `QTimer` 每 3000ms 调 `fetchData` 请求 `/api/latest`。

Q39：UI 怎么支持多主机切换？

A39：
1. 顶部 `QComboBox` 保存当前在线主机列表。
2. 用户切换后触发 `onHostSelected`，按主机重绘各 Tab 数据。
3. 同时显示在线节点数量和状态标签。

Q40：为什么选择 HTTP/JSON 给 Qt，而不是 Qt 直接走 gRPC？

A40：
1. HTTP/JSON 调试门槛低，开发速度快。
2. Qt 的网络请求和 JSON 解析集成成熟。
3. 代价是序列化效率略低，但在看板场景可接受。

Q41：你如何避免图表抖动或视觉噪声？

A41：
1. 固定刷新节奏，统一时间窗口。
2. 对突发尖峰可做平滑（如 EMA）仅用于显示层，不影响原始存储。
3. 单位统一（B/s、KB/s、%）并限制小数位。

### J. 故障排查与真实场景追问

Q42：如果某节点突然“评分断崖式下降”，你怎么排查？

A42：
1. 先看主表中 CPU/内存/负载/磁盘/网络及其 rate 哪项突变。
2. 再钻取明细表：网卡错误丢包、磁盘 util/latency、softirq 哪核飙升。
3. 对比上一轮与历史趋势，判断是瞬时抖动还是持续恶化。
4. 最后结合系统日志、业务日志做根因定位。

Q43：如果 Worker 上报成功率下降，你怎么看？

A43：
1. 先看 gRPC 错误码分布（超时、连接失败、UNAVAILABLE）。
2. 检查 Manager 负载和端口连通性。
3. 评估是否需要重试退避、超时参数、消息压缩。

Q44：如果 MySQL 写入延迟高导致堆积怎么办？

A44：
1. 分离“接收路径”和“落库路径”，加异步队列削峰。
2. 批量写入、连接池、索引优化。
3. 在极端场景可先只保内存缓存，降级落盘频率。

Q45：如果 eBPF 在部分机器加载失败怎么办？

A45：
1. 当前已有自动降级到 `/proc/net/dev`，业务不中断。
2. 记录失败原因（内核版本、权限、BTF 缺失）。
3. 统一做能力探测，按节点标签选择采集策略。

Q46：你如何验证“采集值是正确的”？

A46：
1. 与 `top`、`vmstat`、`iostat`、`sar`、`/proc` 原始数据对比抽样校验。
2. 对速率类指标做守恒检查（非负、异常上限）。
3. 对首次采样空值/零值制定一致策略。

Q47：如何解释“CPU 核数计算”这块实现？

A47：
1. 当前评分代码里 `cpu_cores = info.cpu_stat_size() - 1`，是按现有数据结构做的保守写法。
2. 但若 `cpu_stat` 本身只包含每核数据，这里可能低估核数 1 个。
3. 面试里可主动指出这是可改进点：改为真实逻辑核数，或单独传总核数字段。

Q48：你的时间戳语义是什么？

A48：
1. Manager 写库时用接收时刻（系统时间）格式化为 `DATETIME`。
2. 查询和趋势都基于该时间列。
3. 若要更严谨，可在 Worker 带采样时间并做时钟漂移处理。

Q49：如何做历史趋势与实时数据的结合展示？

A49：
1. 实时看板用 `/api/latest` 拉最新快照。
2. 历史分析走 QueryService 的时间范围查询和趋势聚合。
3. 一条线看“现在”，一条线看“过去”，便于判断异常是否持续。

Q50：你觉得这个项目下一步最值得做的三个增强是什么？

A50：
1. 可靠性：异步上报队列 + 断线重试 + 本地 WAL 缓冲。
2. 查询性能：连接池、批量写入、冷热分层、压缩归档。
3. 智能化：从阈值规则升级到时序模型（如 Prophet/LSTM）并支持告警策略。

### K. 面试官高频“刁钻追问”

Q51：你怎么证明 eBPF 比 `/proc` 更低开销？

A51：
1. 设计 A/B 测试：相同节点、相同周期，对比采集线程 CPU 占用和系统调用次数。
2. 采集前后用 `perf stat`、`pidstat`、`strace -c` 量化。
3. 结论要用数据讲，而不是口头“感觉更快”。

Q52：变化率为什么用相对变化而不是绝对差值？

A52：
1. 相对变化对不同量纲更公平，例如小值突增更敏感。
2. 绝对差值可作为补充，避免基数过小时放大噪声。
3. 最佳实践是二者结合。

Q53：如果某指标上一轮是 0，会导致变化率分母为 0，怎么办？

A53：
1. 你当前实现里分母为 0 时直接返回 0，避免异常。
2. 这是保守策略。
3. 可优化为：返回特殊标记并在异常检测中单独处理“0->正值突增”。

Q54：你的系统如何支持横向扩展到上百节点？

A54：
1. Push 架构天然支持多 Worker 并发接入。
2. 需要增强 Manager：多实例 + 负载均衡 + 共享存储。
3. 进一步可把写库、查询、实时缓存拆成独立服务。

Q55：如果让你重构一版，你会优先改哪里？

A55：
1. 把 `WriteToMysql` 从同步路径剥离，改成异步批处理。
2. 将 SQL 字符串拼接改为预编译，杜绝注入风险。
3. 统一配置中心（采样周期、阈值、白名单网卡）并支持热更新。

---

## 三、你可以这样用这份题库

1. 先按 A~J 每个模块各抽 2 题，做 20 分钟快答训练。
2. 再重点背 Q23/Q28/Q35/Q42/Q47 这类“能体现深度”的题。
3. 最后做一轮“反向追问”：每题都补一句“当前实现 + 可优化点”，面试官通常会给高评价。

如果你愿意，我下一步可以再给你一版“压力面试模式”：只给问题不给答案，你作答后我按真实面试官风格逐题打分和追问。
