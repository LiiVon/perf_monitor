# 可视化 Linux 系统性能监控平台

独立开发者 | 2026.02 - 2026.05

项目背景：面向多服务节点集群，解决缺乏统一性能监控、异常快速定位困难的问题。

项目描述：设计并实现了一套基于 Push 架构的分布式 Linux 性能监控系统，支持多节点 CPU、内存、磁盘、网络、软中断指标采集、评分、持久化与可视化展示。系统采用 C++17 开发，结合 Linux 内核模块零拷贝采集与 eBPF TC Hook 网络监控，实现高性能指标上报；管理端通过 gRPC + HTTP 双协议对外提供数据服务，Qt 6 前端支持多主机切换与实时趋势展示。

技术栈：C++17、Linux 内核模块、mmap、eBPF/libbpf、TC Hook、gRPC、Protobuf、MySQL、Qt 6、cpp-httplib、nlohmann/json、CMake。

主要成果：
1. 自研高性能采集层，开发 `cpu_stat_collector` 内核模块，利用 `mmap` 将 CPU 统计数据零拷贝映射到用户态，结合 `/proc/stat` 回退机制，兼顾采集性能与运行稳定性。
2. 基于 `libbpf` 实现 eBPF TC Hook 网络监控，在网卡 Ingress/Egress 路径原子累加流量与包计数，支持速率计算与计数器回绕处理。
3. 设计 Push 模式分布式通信链路，Worker 周期性推送 `MonitorInfo` 至 Manager，Manager 端通过 gRPC 接收数据并完成主机识别、评分计算、变化率分析与离线节点清理。
4. 构建 MySQL 持久化与查询体系，设计 5 张业务表存储主表、网络、磁盘、内存、软中断明细及变化率数据，支持历史查询、趋势聚合与异常分析。
5. 实现多维加权健康评分模型，综合 CPU、内存、负载、磁盘、网络指标生成主机评分，为调度和异常定位提供统一参考。
6. 基于 Qt 6 + QChart 开发可视化看板，通过 HTTP/JSON 接口实时拉取最新数据，支持 6 个 Tab 页面、多主机切换、3 秒自动刷新与趋势展示。
7. 设计三层自动降级策略，eBPF 加载失败回退 `/proc/net/dev`，内核模块 open 失败回退 `/proc/stat`，确保在非特权环境或能力受限机器上仍可正常运行。

---

# 【优化版】可视化 Linux 系统性能监控平台

独立开发者 | 2026.02 - 2026.05

项目背景：面向多服务节点集群，解决缺乏统一性能监控、异常快速定位困难的问题。

项目描述：设计并实现了一套基于推送架构的分布式 Linux 性能监控系统，支持多节点 CPU、内存、磁盘、网络、软中断指标的实时采集、健康评分、持久化与可视化展示。系统采用 C++17 开发，采集层结合自研内核模块与 eBPF 技术实现高性能指标上报；管理端通过远程调用框架加 HTTP 双协议对外提供 9 类查询接口，前端基于 Qt 6 支持多主机切换与实时趋势展示。

技术栈：C++17、Linux 内核模块、内存映射零拷贝、eBPF、远程过程调用、序列化协议、MySQL、Qt 6、CMake。

主要成果：
1. 自研高性能采集层，开发两个内核模块，通过分配连续物理页并修改进程页表实现零拷贝映射，用户态直接读取内核统计数据，结合延迟工作队列周期采集；用户态优先走内核模块，失败自动降级读系统文件，兼顾性能与兼容性。
2. 基于 eBPF 实现网络流量监控，在网卡收发路径挂载采集程序，通过硬件级原子指令在多核并发场景下安全累加流量计数，采用哈希表按网卡索引存储，支持差值法速率计算与计数器溢出处理。
3. 设计推送模式分布式通信链路，采集端通过独立线程周期性上报数据，采用原子标志位配合逐秒检查实现可中断等待与优雅退出，确保资源安全释放；管理端线程池并发接收，通过回调机制触发业务处理。
4. 实现多维加权健康评分模型（CPU 35% + 内存 30% + 负载 15% + 磁盘 15% + 网络 5%），负载评分引入核心数动态适配，允许 50% 过载才给零分；配合变化率计算与离线主机自动清理，为集群调度与异常定位提供统一参考。
5. 构建数据库持久化与查询体系，设计 5 张业务表覆盖主表与四类明细数据，联合索引支持高效范围查询；管理端实现 9 类查询接口，支持分页、时间聚合降采样、异常检测（四类阈值 + 两级告警）与集群整体统计。
6. 基于 Qt 6 开发可视化看板，通过 HTTP 接口实时拉取数据，支持 6 个功能页面、多主机切换、3 秒自动刷新与趋势展示；读取数据时返回副本而非引用，避免跨线程数据竞争。
7. 设计三层自动降级策略：eBPF 不可用降级读系统文件、内核模块不可用降级读 proc 文件、数据库不可用时仍可编译运行；确保在非特权环境或能力受限机器上仍可正常工作。
8. 采用三层构建体系，协议代码自动生成并打包为静态库供各模块共享；eBPF 与内核模块使用独立编译流程，支持条件编译与多环境部署。